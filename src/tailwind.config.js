module.exports = {
  theme: {
    extend: {
      colors: {
        darkStart: "#0f213d", // Dark mode gradient start
        darkEnd: "#03346A",   // Dark mode gradient end
        lightStart: "#176B87", // Light mode gradient start
        lightEnd: "#DAFFFB",  // Light mode gradient end
        accent: "#64CCC5",    // Accent color for text
        primary: "#021526",   // Primary text color for light mode
      },
    },
  },
};
